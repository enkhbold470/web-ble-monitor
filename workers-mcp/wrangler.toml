# MCP-only Worker — separate from the OpenNext app in repo root (`wrangler.jsonc`).
#:schema node_modules/wrangler/config-schema.json
name = "web-ble-monitor-mcp"
main = "src/index.ts"
compatibility_date = "2026-04-30"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true
